#include<bits/stdc++.h>
#include<vector>
using namespace std;

int main(){
    vector<string>s{"This" , "is" , "geeksforgeeks"};
    for(auto it = s.begin(); it != s.end();++it){
        cout<<*it<<' ';
        }
}
