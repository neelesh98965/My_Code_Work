#include<bits/stdc++.h>
using namespace std;

int main(){
    string s;
    getline(cin,s);
    string a;
    int c =count(s.begin(),s.end(),' ');
    remove(s.begin(),s.end(),' ');
    s.resize(s.length()-c);
    cout<<s;
    cout<<s.length();
}
