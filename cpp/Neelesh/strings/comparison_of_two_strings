#include<iostream>
#include<string.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
   cin.tie(NULL);
    char s[100],s1[100];
    cin>>s>>s1;
    if(strcmp(s,s1) == 0){
        cout<<"The stirngs are equal";
    }
    else{
        cout<<"The strings are not equal";
    }
    return 0;
}
