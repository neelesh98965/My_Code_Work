#include<bits/stdc++.h>
using namespace std;
void count_bits_hack(int n){
    int count=0;
    while(n>0){
        n = n&(n-1);
        count++;
    }
    cout<<count;
}
int main(){
    int n;
    cin>>n;
    count_bits_hack(n);
    
}
