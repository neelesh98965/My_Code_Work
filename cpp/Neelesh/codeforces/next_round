#include<bits/stdc++.h>
using namespace std;

int main(){
    int a,b;
    cin>>a>>b;
   int arr[a],count = 0;
   for(int i=0;i<a;i++){
       cin>>arr[i];
   } 
   for(int i=0;i<a;i++){
       if((arr[i] > 0) && (arr[i] >= arr[b-1])){
           count++;
       }
   }
       cout<<count;
}
