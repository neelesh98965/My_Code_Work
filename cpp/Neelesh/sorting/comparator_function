#include<bits/stdc++.h>
using namespace std;

bool compare(int a,int b){
    return a>b;
}

int main(){
    int arr[] = {5,4,3,2,1,98,65,76,90};
    int n = sizeof(arr)/sizeof(int);
    sort(arr,arr+n,compare);
    for(auto x:arr){
        cout<<x<<" ";
    }
}
