#include<iostream>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
   cin.tie(NULL);
    int n,count = 0;
    cin>>n;
    while (n)
    {
        count += n%10;
        n = n/10;
    }
    cout<<count;
    return 0;
}
