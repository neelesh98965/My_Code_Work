#include<iostream>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
   cin.tie(NULL);
    char ch;
    cin>>ch;
    cout<<"The ASCII value of the character is "<<int(ch);
    return 0;
}
